var React = require('react'),
    mui = require('material-ui');

var Inbox = React.createClass({
    render: function() {
        return (
            <div className="inbox">
                <h1>This is Inbox</h1>
            </div>
        );
    }
});

module.exports = Inbox;